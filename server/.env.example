# ========== 必填 ==========
# 运行环境: production / development
ENV=production

# JWT 密钥（生产环境必须设置，建议 32+ 位随机字符串）
# 生成方式: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# 管理员 Token（用于创建/删除公告等管理操作）
ADMIN_TOKEN=

# ========== 可选 ==========
# Mod-Server 通信密钥（MC服务端Mod调用后端API时验证身份）
# 如果设置，需要同步配置到 mcserver/config/mclauncher-auth.json
MOD_API_KEY=

# 数据库连接（默认 SQLite，生产环境建议 MySQL）
# SQLite: sqlite:///./mclauncher.db
# MySQL:  mysql+pymysql://user:pass@localhost:3306/mclauncher
DATABASE_URL=sqlite:///./mclauncher.db

# CORS 允许的源（逗号分隔，默认 *）
# 示例: http://localhost:5173,https://yourdomain.com
CORS_ORIGINS=*

# 同步配置文件路径
SYNC_CONFIG_FILE=./sync_config.json

# Mods 目录和清单
MODS_DIR=./mods
MODS_MANIFEST=./mods_manifest.json

# 客户端整包目录
CLIENT_PACK_DIR=./client_pack
